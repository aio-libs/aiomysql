[build-system]
requires = [
    "setuptools >= 64, < 70",
    "setuptools_scm[toml] >= 8",
    "wheel",
]
build-backend = "setuptools.build_meta"


[tool.setuptools_scm]
write_to = "aiomysql/_scm_version.py"
