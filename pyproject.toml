[build-system]
requires = [
    # Essentials
    "setuptools >= 45",

    # Plugins
    "setuptools_scm[toml] >= 7.0.5",
]
build-backend = "setuptools.build_meta"

[tool.setuptools_scm]
write_to = "aiomysql/_scm_version.py"
