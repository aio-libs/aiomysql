#
# The MySQL database server configuration file.
#
[client]
user = {user}
socket = {unix_socket}
password = {password}
database = {db}
default-character-set = utf8

[client_with_unix_socket]
user = {user}
socket = {unix_socket}
password = {password}
database = {db}
default-character-set = utf8
