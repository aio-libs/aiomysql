#
# The MySQL database server configuration file.
#
[client]
user = {user}
port = {port}
host = {host}
password = {password}
database = {db}
default-character-set = utf8

[client_with_unix_socket]
user = {user}
port = {port}
host = {host}
password = {password}
database = {db}
default-character-set = utf8
